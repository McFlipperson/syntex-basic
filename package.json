{
  "name": "syntex",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "dev:vercel": "pnpm --filter @syntex/vercel dev",
    "dev:widget": "pnpm --filter @syntex/widget dev"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
